jest.setTimeout(100)
describe('build-raptor-action', () => {
  test('lorem ipsum', () => {
    expect(5).toEqual(5)
  })
  test('1', () => {
    expect(1).toEqual(1)
  })
  test('5', () => {
    expect(5).toEqual(5)
  })
  test('8', () => {
    expect(8).toEqual(8)
  })
})
